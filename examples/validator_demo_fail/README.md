# Validator failing example demo

This active inventory entry intentionally references a missing
`benchmarkingjob.yaml`. It exists only to demonstrate that a newly added
Example with a blocking validation failure is classified as an Added Example
and a PR regression.
